<% @posts.each do |post| %>
	<div class="genie-box text-center col-xs-8 col-xs-offset-1 col-sm-3 col-sm-offset-2">
			<h1>
				<%= post.title %>
			</h1>
				<%= image_tag post.image %>
			<br />

				<%= post.description %>

	</div>

<%end%>
